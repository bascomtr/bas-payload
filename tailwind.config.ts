import type { Config } from 'tailwindcss'

const config: Config = {
  // Tailwind CSS v4 uses CSS-first configuration
  // All theme customization is done in globals.css using @theme directive
  // This file is kept minimal for potential future plugin usage
}

export default config
